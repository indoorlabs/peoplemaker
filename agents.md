# AI Agent Instructions

- 가능한 한 한국어로 자세히 설명해주세요.
- 사용자는 시니어 개발자입니다.
- 사용자는 현재 코드를 직접 보지 않고 작업을 지시하고 있으므로, 코드의 변화나
  구조를 상세히 설명해야 합니다.
- 지시하지 않은 리팩토링이나 코드 구조 변경을 독단적으로 수행하지 마십시오.
- 대규모 변경이나 위험 요소가 있는 수정 전에는 반드시 사용자에게 먼저 확인을
  구하십시오.

## 이 저장소의 규약 (urbanspace 와 같다)

1. **순수 층** — `src/lib/*.mjs` 는 three.js·DOM·PyTorch 를 import 하지
   않는다. 값과 규칙만 두고, 어댑터(웹 재생기·파이썬 파이프라인)가 그것을
   읽는다. 그래야 게이트가 브라우저 없이 검사한다.

2. **값 옆에 손으로 베낀 수를 두지 않는다** — 인체치수는 `source`·`years`·
   `population`·`percentile` 이 함께 있어야 하고, 없으면 `pending` 으로 남긴다.
   **비율로 만들어 채우지 않는다** (눈높이 = 키 × 0.93 같은 것). 게이트가 막는다.

3. **게이트가 기본** — `scripts/check-*.mjs` 를 쓰면 그 순간부터 검사된다
   (등록 단계 없음). 새로 생긴 결함만 빌드를 막고, 알려진 결함은 기준선에 잠근다.

4. **게이트를 쓰면 일부러 깨서 확인한다.** 검사를 쓴 뒤 코드를 한 군데씩
   망가뜨려 잡히는지 본다. "값이 범위 안인가" 대신 **"입력을 흔들면 출력이
   따라 움직이는가"** 를 묻는다 — urbanspace 에서 그 차이로 네 번 뚫렸다.

5. **계약이 유일한 외부 표면** — `src/lib/motionPack.mjs`. spacemaker 와
   urbanspace 는 이것만 본다. 여기에 없는 것을 밖에서 쓰게 하지 않는다.

## 하지 않기로 한 것

- **배치·동선·활동 스케줄**은 여기서 안 한다. 공간을 아는 쪽의 일이다.
- **SMPL/SMPL-X 토폴로지를 계약의 규약으로 삼지 않는다.** 상업 라이선스가
  계약에 박힌다 (2026-02 Epic 의 Meshcapade 인수). 안에서 도구로 쓰는 것은
  괜찮지만, 내보내는 팩은 mixamo 또는 VRM 규약이다.
- **물리 기반 제어(ProtoMotions/PHC)와 장면 인지 생성**은 P3 까지 안 붙인다.
  고른 클립을 정합하는 문제로 두는 것이 비용 대비 옳다.

## 커밋

무엇을 왜 했는지, 그리고 **잰 수**를 적는다. "고쳤다" 가 아니라 "무엇이
몇 개에서 몇 개가 됐다".
